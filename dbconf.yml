development:
  driver: postgres
  dsn: postgres://localhost/drachma?sslmode=disable

test:
  driver: postgres
  dsn: postgres://localhost/drachma_test?sslmode=disable

docker:
  driver: postgres
  dsn: postgres://gotoschool:goto1234@postgres:5432/drachma?sslmode=disable

kubernetes:
  driver: postgres
  dsn: postgres://gotoschool:goto1234@localhost:5432/drachma?sslmode=disable
